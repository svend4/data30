# Каталог вертикалей, сценариев и пайплайнов

Ниже приведены 8 вертикалей с 5 сценариями каждая. Для каждого сценария описаны шаги пайплайна (шаги → входы → выходы), соответствующие функции из MVP‑словаря и приложения из каталога.

## Таблица «Сценарий → Шаги → Функции → Приложения»

| Вертикаль | Сценарий | Шаги (входы → выходы) | Функции | Приложения |
| --- | --- | --- | --- | --- |
| Маркетинг | Лидогенерация из веб‑форм | 1) Импорт CSV лидов (CSV → таблица). 2) Очистка email/телефонов (email/phone → normalized). 3) Валидация контактов (normalized → valid). 4) Дедупликация (таблица → очищенная таблица). 5) Загрузка в CRM‑лист (таблица → rows inserted). | read_csv; normalize_email; normalize_phone; validate_email; validate_phone; deduplicate_rows; insert_rows | Notion; Slack |
| Маркетинг | Сегментация базы | 1) Импорт базы (CSV → таблица). 2) Фильтрация по признакам (таблица → сегмент). 3) Группировка по городам/каналам (таблица → агрегаты). 4) Экспорт сегмента (таблица → CSV). | read_csv; filter_rows; group_by; write_csv | Notion; Confluence |
| Маркетинг | Персонализация контента | 1) Загрузка шаблонов (JSON → структура). 2) Выделение сущностей из брифа (текст → сущности). 3) Подстановка по шаблону (сущности → готовый текст). 4) Экспорт финала (данные → JSON). | read_json; extract_entities; replace_regex; write_json | Notion; Confluence |
| Маркетинг | Отчет по эффективности кампаний | 1) Импорт данных кампаний (CSV → таблица). 2) Агрегация показателей (таблица → суммы/средние). 3) Вычисление перцентилей (таблица → значения). 4) Генерация отчета (данные → файл). | read_csv; aggregate_sum; aggregate_avg; calculate_percentile; generate_report | Confluence; Slack |
| Маркетинг | Мониторинг упоминаний | 1) Сбор текстов (HTTP → тексты). 2) Определение языка (текст → lang). 3) Классификация (текст → label). 4) Отправка алерта (label → уведомление). | http_get; detect_language; classify_text; send_email | Slack; Microsoft Teams |
| Продажи | Обработка входящих лидов | 1) Импорт лидов (CSV → таблица). 2) Нормализация контактов (email/phone → normalized). 3) Валидация (normalized → valid). 4) Запись в таблицу (таблица → rows inserted). | read_csv; normalize_email; normalize_phone; validate_email; validate_phone; insert_rows | Notion; Slack |
| Продажи | Квалификация лидов | 1) Фильтрация по статусу (таблица → shortlist). 2) Сортировка по скору (таблица → отсортировано). 3) Присвоение задач менеджерам (таблица → задачи). | filter_rows; sort_rows; publish_queue | Jira; Asana |
| Продажи | Подготовка коммерческого предложения | 1) Импорт шаблона (JSON → структура). 2) Подстановка данных клиента (данные → документ). 3) Экспорт PDF/отчета (данные → файл). | read_json; replace_regex; generate_report | Confluence; Notion |
| Продажи | Воронка продаж по этапам | 1) Импорт сделок (CSV → таблица). 2) Группировка по этапам (таблица → агрегаты). 3) Расчет конверсии (таблица → метрики). 4) Экспорт отчета (метрики → файл). | read_csv; group_by; aggregate_avg; generate_report | Confluence; Slack |
| Продажи | Планирование встреч с клиентами | 1) Формирование списка клиентов (таблица → список). 2) Отправка приглашений (контакты → уведомления). 3) Планирование слотов (cron → job). | filter_rows; send_email; schedule_job | Google Meet; Zoom; Microsoft Teams |
| HR | Подбор кандидатов | 1) Импорт резюме (CSV → таблица). 2) Извлечение сущностей (текст → навыки). 3) Классификация профиля (текст → label). 4) Запись в базу кандидатов (таблица → rows inserted). | read_csv; extract_entities; classify_text; insert_rows | Notion; Confluence |
| HR | Онбординг сотрудника | 1) Создание задач (данные → задачи). 2) Планирование встреч (cron → job). 3) Отправка приветственного письма (контакт → уведомление). | publish_queue; schedule_job; send_email | Asana; Slack; Microsoft Teams |
| HR | Оценка эффективности | 1) Импорт результатов (CSV → таблица). 2) Агрегация показателей (таблица → суммы/средние). 3) Поиск выбросов (таблица → outliers). 4) Отчет (данные → файл). | read_csv; aggregate_sum; aggregate_avg; detect_outliers; generate_report | Confluence; Notion |
| HR | Оплата и компенсации | 1) Импорт данных выплат (CSV → таблица). 2) Проверка пропусков (таблица → nulls). 3) Заполнение пропусков (таблица → заполнено). 4) Экспорт реестра (таблица → CSV). | read_csv; detect_nulls; fill_missing; write_csv | Notion; Confluence |
| HR | Обучение и развитие | 1) Сбор заявок (HTTP → заявки). 2) Группировка по программам (таблица → агрегаты). 3) Планирование сессий (cron → job). | http_get; group_by; schedule_job | Zoom; Google Meet |
| Финансы | Ежемесячное закрытие | 1) Импорт проводок (CSV → таблица). 2) Сверка и объединение (таблицы → объединено). 3) Вычисление итогов (таблица → суммы). 4) Отчет (данные → файл). | read_csv; merge_datasets; aggregate_sum; generate_report | Confluence; Notion |
| Финансы | Контроль бюджета | 1) Импорт бюджета (CSV → таблица). 2) Импорт факта (CSV → таблица). 3) Соединение по статьям (таблицы → объединено). 4) Расчет отклонений (таблица → метрики). | read_csv; join_tables; aggregate_sum; calculate_percentile | Confluence; Slack |
| Финансы | Прогноз cash‑flow | 1) Импорт исторических данных (CSV → таблица). 2) Нормализация дат (дата → ISO). 3) Агрегация по периодам (таблица → агрегаты). 4) Экспорт прогноза (таблица → CSV). | read_csv; parse_date; group_by; write_csv | Confluence; Notion |
| Финансы | Проверка платежей | 1) Импорт реестра (CSV → таблица). 2) Проверка валидности реквизитов (данные → valid). 3) Хэширование для контроля (данные → hash). 4) Экспорт сверки (таблица → CSV). | read_csv; validate_json_schema; hash_sha256; write_csv | Confluence |
| Финансы | Отчетность для руководства | 1) Сбор данных (CSV → таблица). 2) Агрегация KPI (таблица → метрики). 3) Генерация отчета (данные → файл). 4) Отправка уведомления (файл → сообщение). | read_csv; aggregate_avg; generate_report; send_email | Confluence; Slack |
| Документооборот | Регистрация входящих договоров | 1) Импорт реестра (CSV → таблица). 2) Нормализация дат (строки → ISO). 3) Запись в хранилище (таблица → rows inserted). 4) Уведомление ответственных (данные → сообщение). | read_csv; parse_date; insert_rows; send_email | Confluence; Microsoft Teams |
| Документооборот | Согласование документа | 1) Создание задачи на согласование (данные → задача). 2) Отправка уведомлений (задача → сообщение). 3) Отслеживание статуса (job → status). | publish_queue; send_email; get_job_status | Jira; Slack |
| Документооборот | Контроль версий | 1) Экспорт версии (данные → файл). 2) Контрольная сумма (файл → checksum). 3) Запись в журнал (данные → rows inserted). | generate_report; compute_checksum; insert_rows | Confluence; GitHub |
| Документооборот | Авто‑сбор пакета для сделки | 1) Поиск документов (query → документы). 2) Объединение наборов (документы → пакет). 3) Экспорт пакета (данные → файл). | search_text; merge_datasets; generate_report | Confluence; Notion |
| Документооборот | Архивирование | 1) Экспорт архива (данные → файл). 2) Загрузка в хранилище (файл → status). 3) Запись метаданных (данные → rows inserted). | generate_report; export_s3; insert_rows | Confluence; GitLab |
| Поддержка клиентов | Обработка обращений | 1) Импорт тикетов (CSV → таблица). 2) Классификация тем (текст → label). 3) Маршрутизация (label → очередь). 4) Уведомление операторов (очередь → сообщение). | read_csv; classify_text; publish_queue; send_email | Slack; Microsoft Teams |
| Поддержка клиентов | SLA мониторинг | 1) Импорт логов (CSV → таблица). 2) Расчет перцентилей (таблица → значения). 3) Детект выбросов (таблица → outliers). 4) Отчет (данные → файл). | read_csv; calculate_percentile; detect_outliers; generate_report | Confluence; Slack |
| Поддержка клиентов | База знаний | 1) Поиск решений (query → записи). 2) Нормализация текста (текст → clean). 3) Запись обновлений (данные → rows inserted). | search_text; trim_whitespace; insert_rows | Confluence; Notion |
| Поддержка клиентов | Анализ тональности | 1) Импорт отзывов (CSV → таблица). 2) Определение языка (текст → lang). 3) Классификация (текст → label). 4) Агрегация по меткам (таблица → агрегаты). | read_csv; detect_language; classify_text; group_by | Confluence; Slack |
| Поддержка клиентов | Отчет по обращениям | 1) Импорт тикетов (CSV → таблица). 2) Агрегация по категориям (таблица → агрегаты). 3) Генерация отчета (данные → файл). | read_csv; group_by; generate_report | Confluence; Notion |
| IT/DevOps | Мониторинг инцидентов | 1) Сбор событий (HTTP → события). 2) Фильтрация по критичности (таблица → инциденты). 3) Уведомление команды (инциденты → сообщение). | http_get; filter_rows; send_email | Slack; Microsoft Teams |
| IT/DevOps | Управление релизами | 1) Сбор задач релиза (таблица → список). 2) Группировка по сервисам (таблица → агрегаты). 3) Планирование запуска (cron → job). 4) Отчет по статусу (данные → файл). | filter_rows; group_by; schedule_job; generate_report | Jira; GitHub |
| IT/DevOps | CI/CD контроль качества | 1) Импорт логов (CSV → таблица). 2) Поиск выбросов (таблица → outliers). 3) Генерация отчета (данные → файл). | read_csv; detect_outliers; generate_report | GitHub; GitLab |
| IT/DevOps | Управление уязвимостями | 1) Импорт результатов сканирования (CSV → таблица). 2) Классификация по критичности (текст → label). 3) Создание задач (label → очередь). | read_csv; classify_text; publish_queue | Jira; GitLab |
| IT/DevOps | Планирование обслуживания | 1) Сбор заявок (HTTP → заявки). 2) Сортировка по приоритету (таблица → сортировано). 3) Планирование работ (cron → job). | http_get; sort_rows; schedule_job | Jira; Asana |
| Аналитика | Когортный анализ | 1) Импорт событий (CSV → таблица). 2) Расчет ретенции (таблица → retention). 3) Экспорт отчета (данные → файл). | read_csv; calculate_retention; generate_report | Confluence; Notion |
| Аналитика | Рекомендации товаров | 1) Импорт истории покупок (CSV → таблица). 2) Генерация рекомендаций (данные → items). 3) Отправка в канал (items → сообщение). | read_csv; recommend_items; send_email | Slack; Microsoft Teams |
| Аналитика | Поиск трендов | 1) Импорт текстов (CSV → таблица). 2) Токенизация (текст → tokens). 3) Стемминг (tokens → tokens). 4) Агрегация частот (таблица → агрегаты). | read_csv; tokenize_text; stem_text; group_by | Confluence; Notion |
| Аналитика | Гео‑аналитика | 1) Импорт адресов (CSV → таблица). 2) Геокодирование (адрес → координаты). 3) Экспорт результата (таблица → CSV). | read_csv; geo_enrich; write_csv | Notion; Confluence |
| Аналитика | Контроль качества данных | 1) Импорт набора (CSV → таблица). 2) Поиск null (таблица → nulls). 3) Дедупликация (таблица → очищено). 4) Отчет (данные → файл). | read_csv; detect_nulls; deduplicate_rows; generate_report | Confluence; Notion |
